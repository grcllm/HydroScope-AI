"""Agentic runtime for dpwh_agent using Google Gen AI (Gemini).

This package provides:
- A lightweight Gemini client wrapper
- A registry of callable tools that map to the existing analytics

The tools are designed to be passed directly to the google-genai SDK
for automatic function calling.
"""
